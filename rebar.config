{erl_opts, [debug_info]}.
{deps, []}.

% NOTE: Datetime and timestamp are disabled by default for performance reasons.
%       Enable globally via rebar.config as {erl_opts, [{d, 'EUNEUS_ENABLE_CALENDAR'}]}.
{eunit_compile_opts, [{d, 'EUNEUS_ENABLE_CALENDAR'}]}.
